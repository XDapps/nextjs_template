/**
 * AI / LLM stub — replace with your preferred AI SDK.
 *
 * Common choices: Vercel AI SDK (`ai`), Anthropic SDK (`@anthropic-ai/sdk`),
 * OpenAI SDK (`openai`)
 *
 * TODO: install @yourorg/ai or a standard SDK, then replace this file.
 * Example: `npm i ai @ai-sdk/openai`
 */

/** Returns the AI client. Replace this stub with your actual SDK client. */
export function getAi(): never {
  throw new Error(
    "getAi() is not implemented — see lib/ai.ts. TODO: install your AI SDK (e.g. npm i ai @ai-sdk/openai) and replace this stub.",
  );
}
