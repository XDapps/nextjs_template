# Project instructions

This is a Next.js template repo. On first clone, follow the setup runbook at [AGENT-SETUP.md](./AGENT-SETUP.md) before writing any code — it walks through customizing brand, colors, font, and radius via `design.config.ts` and `app/globals.css`.

Next.js 16–specific rules:

@AGENTS.md
